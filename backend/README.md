# Activer l'environement backend

```bash
cd backend
source .venv/bin/activate
pip install -r requirements.txt
```

# Lancer l'API
```bash
fastapi dev app/main.py
```

On peut aller voir `http://127.0.0.1:8000/docs` pour avoir la doc API swagger UI